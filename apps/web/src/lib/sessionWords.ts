/** Session words shared by server and client components. */
export const KIND_WORD: Record<"PRE" | "REGULAR" | "LUNCH" | "POST" | "CLOSED", string> = {
  PRE: "Pre-market", REGULAR: "Regular session", LUNCH: "Lunch break", POST: "After hours", CLOSED: "Closed",
};
