# Judge checklist

Five minutes, no install. Ten more with a wallet. Automated evaluators: start at https://www.usekerb.xyz/llms.txt.

1. **What it is.** https://www.usekerb.xyz: credit on the market's clock. A sixty-second guided tour is under *Docs → Take the tour*.
2. **Live on mainnet.** `/proof`: the mainnet risk plane with the time of the last post; follow "KerbTerms on OKLink".
3. **Builder Code.** `/proof`, "Latest Terms posts": each row decodes `kt0hl6xyhlx8xmt` from that transaction's calldata.
4. **The clock moves the credit.** `/asset/KOx`, "Terms history": Carry and Session Max step at each session change; "What changed" says why, with numbers.
5. **Every term explains itself.** `/asset/HKEXCx`, "Why these terms": three computed sentences from the post's own inputs.
6. **The exit is measured.** `/asset/HKEXCx`, Liquidity: the tick-walk against the OKX DEX quote, the bound used, and 72 hours of checks.
7. **Recompute one.** `/proof` "Recompute" tile, or from a clone with no database: `pnpm --filter @kerb/engine kerb verify <inputsHash>` reads the posted values from KerbTerms on chain.
8. **Borrow and cure.** `/credit` with any browser wallet on X Layer testnet (test OKB from https://www.okx.com/xlayer/faucet): mint, deposit, Session Max, borrow. At the demo Last Call (hh:53:58 UTC every hour) cure the keeper's standing position from "Curable now".
9. **Contracts and agents.** `/developers`: KerbQuote on mainnet (one view call), ten Credit Mark feeds, the x402 credit check (HTTP 402, one cent in USDT0 on X Layer), the MCP server, `npm i kerb-sdk`.
10. **Measured research.** `/research/1` (a closed weekend) and `/research/2` (the end of the X Liquidity incentives).
11. **Repository.** https://github.com/Franlinozz/Kerb: CI badge, README with the system diagram, `docs/API.md`, `docs/v2/KTS-0.2.md`, `docs/release/CLAIM_EVIDENCE.md`, tests.
