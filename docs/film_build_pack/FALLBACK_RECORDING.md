# Fallback Recording Plan

Claude may create replacement captures only when the source audit marks a clip below acceptance quality.

## Public route replacements

These can be recorded autonomously:

Home:
`https://www.usekerb.xyz/`

Board:
`https://www.usekerb.xyz/board`

Methodology:
`https://www.usekerb.xyz/methodology`

Proof:
`https://www.usekerb.xyz/proof`

Developers:
`https://www.usekerb.xyz/developers`

Research:
`https://www.usekerb.xyz/research/2`

Asset:
select the same asset used in the final story if possible.

## Capture protocol

1920x1080 viewport when possible.

Dark theme.

100 percent browser zoom.

Hide bookmarks bar.

Disable notifications.

Wait until live data settles.

Record:
- 4 seconds still before interaction
- one deliberate interaction
- 4 seconds still after interaction

Do not perform decorative scrolling.

## Credit replacement

Do not autonomously create new real-money actions.

For X Layer testnet, a replacement may be staged only if the operator has already supplied the required test wallet configuration and the environment clearly targets chain 1952.

If no prepared environment exists, salvage the existing 05 and 06 footage rather than improvising.

## Last Call replacement

Only if both 06 and 06b fail.

Required result:
- Session Max position
- visible countdown or state transition
- Curable now
- Cure from another wallet
- transaction confirmation
- post-Cure state

Record the transition and Cure as two separate raw takes if that improves reliability. The final edit may join them with a clear cut.
